## **📝 일일 문제 분석 및 풀이 노트**

### **[문제 이름/번호] 분석**
https://www.codetree.ai/ko/trails/complete/curated-cards/test-max-area-of-positive-rectangle/description
### 양수직사각형의 최대 크기

### **1. 문제 핵심 요약**

- [ ] 입력은 무엇인가? (자료형, 개수):
- [ ] 출력은 무엇인가? (포맷, 정답 조건):
- [ ] 문제의 목표를 한 문장으로 정리하면?:

### **2. 제약 조건 (Constraints) 분석**

- 시간 제한: __초 (약 __억 번 연산 가능)
- 메모리 제한: ____MB
- 입력 크기(N):
    - 최소:
    - 최대:
- 값의 범위: (Integer 범위 초과 여부 확인)

### **3. 알고리즘 선택 & 복잡도 예상**

- [ ] 예상 시간 복잡도: O( N^2 )
- [ ] 선택한 자료구조: 배열, 스택
- [ ] 선택한 알고리즘: (Greedy, DP, BFS/DFS, 이분탐색 등) 브루트포스
- [ ] 근거: (N의 크기를 볼 때 이 알고리즘이 적합한가?)

### **4. 수학적/논리적 증명 및 설계 (Core Logic)**

> 코드로 바로 넘어가기 전, 문제를 해결하는 핵심 로직을 수학적 공식이나 논리적 증명으로 먼저 구성합니다.

- **수식 또는 상태 전이 방정식:** 히스토그램을 이용한 넓이 구하기
- **논리적 타당성 증명:**

### **6. 로직 설계 (Pseudo-code)**
 각 행마다 누적된 양수의 개수를 저장
 히스토그램에서 사각형의 최대 넓이를 구하는 함수에 입력 후 최대값 비교

최대 넓이 함수()
스택 생성
입력된 배열의 길이+1 만큼 for문
배열에 저장된 높이값이 스택에 저장된 값보다 작다면 넓이를 계산 이때 pop
peek로 값을 가져와 가로 길이를 구하고 높이와 곱해서 넓이를 계산
스택이 비어있거나 배열에서 받아온 높이값이 더 크다면 스택에 push해주고 다음 반복 실행

---

### **💻 코드 구현 및 피드백**

### **내 코드 (초안)**

**코드 스니펫**

```java
import java.io.*;
import java.util.*;

public class Main {

    static int N, M;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        N = Integer.parseInt(st.nextToken());
        M = Integer.parseInt(st.nextToken());

        int[][] arr = new int[N][M];

        for(int i = 0; i < N; i++){
            st = new StringTokenizer(br.readLine());
            for(int j = 0; j < M; j++){
                arr[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        int[] height = new int[M];
        int maxArea = -1; 

        for(int i = 0; i < N; i++){

            for(int j = 0; j < M; j++){
                if(arr[i][j] > 0)
                    height[j]++;
                else
                    height[j] = 0;
            }

            int area = largestRectangle(height);
            if(area > 0) {              
                maxArea = Math.max(maxArea, area);
            }
        }

        System.out.println(maxArea);
    }

    public static int largestRectangle(int[] height) {

        Stack<Integer> stack = new Stack<>();
        int maxArea = 0;

        for(int i = 0; i <= height.length; i++){

            int h = (i == height.length) ? 0 : height[i];

            while(!stack.isEmpty() && height[stack.peek()] > h){

                int top = stack.pop();
                int width;

                if(stack.isEmpty())
                    width = i;
                else
                    width = i - stack.peek() - 1;

                maxArea = Math.max(maxArea, height[top] * width);
            }

            stack.push(i);
        }

        return maxArea;
    }
}
```

