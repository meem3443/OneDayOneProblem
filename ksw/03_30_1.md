## **📝 일일 문제 분석 및 풀이 노트**

### **[문제 이름/번호] 분석**
https://www.codetree.ai/ko/trails/complete/curated-cards/intro-conveyor-belt/description
### 컨베이어 벨트

### **1. 문제 핵심 요약**

- [ ] 입력은 무엇인가? (자료형, 개수):
- [ ] 출력은 무엇인가? (포맷, 정답 조건):
- [ ] 문제의 목표를 한 문장으로 정리하면?: 배열 이동시킨걸 출력

### **2. 제약 조건 (Constraints) 분석**

- 시간 제한: __초 (약 __억 번 연산 가능)
- 메모리 제한: ____MB
- 입력 크기(N):
    - 최소:
    - 최대:
- 값의 범위: (Integer 범위 초과 여부 확인)

### **3. 알고리즘 선택 & 복잡도 예상**

- [ ] 예상 시간 복잡도: O( N )
- [ ] 선택한 자료구조: Deque
- [ ] 선택한 알고리즘: (Greedy, DP, BFS/DFS, 이분탐색 등)
- [ ] 근거: (N의 크기를 볼 때 이 알고리즘이 적합한가?) 배열을 사용할때 보다 빠를 것 같음


### **6. 로직 설계 (Pseudo-code)**
두개의 덱을 만들고 각각 마지막 원소를 다른 덱 첫번째에 추가

---

### **💻 코드 구현 및 피드백**

### **내 코드 (초안)**

**코드 스니펫**

```java
import java.io.*;
import java.util.*;
public class Main {
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        StringBuilder sb = new StringBuilder();

        int N = Integer.parseInt(st.nextToken());
        int T = Integer.parseInt(st.nextToken());
        Deque<Integer> d1 = new ArrayDeque<>();
        Deque<Integer> d2 = new ArrayDeque<>();

        st = new StringTokenizer(br.readLine());
        while(st.hasMoreTokens()){
            d1.addLast(Integer.parseInt(st.nextToken()));
        }
            
        st = new StringTokenizer(br.readLine());
        while(st.hasMoreTokens()){
            d2.addLast(Integer.parseInt(st.nextToken()));
        }
        
        for(int i = 0; i < T; i++){
            d2.addFirst(d1.pollLast());
            d1.addFirst(d2.pollLast());
        }
        while(!d1.isEmpty()){
           sb.append(d1.pollFirst()).append(" ");
        }
        sb.append("\n");
        while(!d2.isEmpty()){
           sb.append(d2.pollFirst()).append(" ");
        }

        System.out.println(sb);
        
        
    }
}
```


### **AI 최적화 코드 비교 및 진단**

- **개선된 코드의 효율성 차이:** (시간/공간 복잡도 측면에서 어떻게 개선되었는가?)
- **자가 진단:** 이 문제에서 내가 정확히 이해하지 못했던 개념이나 논리적 맹점은 무엇이었는가?