## **📝 일일 문제 분석 및 풀이 노트**

### **[문제 이름/번호] 분석**
https://www.codetree.ai/ko/trails/complete/curated-cards/challenge-tromino/description
### 트로미노
### **1. 문제 핵심 요약**

- [ ] 입력은 무엇인가? (자료형, 개수): 정수 2개, 정수형 2차원 배열
- [ ] 출력은 무엇인가? (포맷, 정답 조건): 특정 모양으로 묶었을 때의 최대값
- [ ] 문제의 목표를 한 문장으로 정리하면?: 2차원 배열 내에서 최대값 찾기

### **2. 제약 조건 (Constraints) 분석**

- 시간 제한: 3000ms
- 메모리 제한: 176MB
- 입력 크기(N): 3<= N, M<= 200   1 <= 배열내에 주어지는 수 <= 1000
    - 최소: 3
    - 최대: 200
- 값의 범위: (Integer 범위 초과 여부 확인) 초과안함

### **3. 알고리즘 선택 & 복잡도 예상**

- [ ] 예상 시간 복잡도: O( N^2 )
- [ ] 선택한 자료구조: 이차원 배열
- [ ] 선택한 알고리즘: (Greedy, DP, BFS/DFS, 이분탐색 등) 브루트 포스
- [ ] 근거: (N의 크기를 볼 때 이 알고리즘이 적합한가?)


### **6. 로직 설계 (Pseudo-code)**
 입력을 받아 정수형 이차원 배열 생성
 주어진 그림대로 탐색을 하기 위해 범위가 다른 for문 3개 생성
 반복문 루프를 한번 돌때마다 max의 값과 비교해서 더 큰 값을 max에 저장
---

### **💻 코드 구현 및 피드백**

### **내 코드 (초안)**

**코드 스니펫**

```java
import java.io.*;
import java.util.*;

public class Main {
    static int N, M;
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        
        N = Integer.parseInt(st.nextToken());
        M = Integer.parseInt(st.nextToken());
        int[][] arr = new int[N][M];
        int max1 = -1;
        int max2 = -1;
        int max3 = -1;
        int sum1, sum2, sum3, sum4, vartical, horizen;
        
        for(int i = 0; i < N; i++){
            st = new StringTokenizer(br.readLine());
            for(int j = 0; j < M; j++){
                arr[i][j] = Integer.parseInt(st.nextToken());
            }
        }
        
        for(int i = 0; i < N-1; i++){
            for(int j = 0; j < M-1; j++){
                sum1 = arr[i][j]+arr[i][j+1]+arr[i+1][j];
                sum2 = arr[i][j]+arr[i+1][j]+arr[i+1][j+1];
                sum3 = arr[i][j+1]+arr[i+1][j]+arr[i+1][j+1];
                sum4 = arr[i][j]+arr[i][j+1]+arr[i+1][j+1];

                max1 = Math.max(max1, Math.max(sum1, Math.max(sum2, Math.max(sum3, sum4))));
            }
        }

        for(int i = 0; i < N-2; i++){
            for(int j = 0; j < M; j++){
                vartical = arr[i][j]+arr[i+1][j]+arr[i+2][j];
                max2 = Math.max(max2, Math.max(max2, vartical));
            }
        }
        for(int i = 0; i < N; i++){
            for(int j = 0; j < M-2; j++){
                horizen = arr[i][j]+arr[i][j+1]+arr[i][j+2];
                max3 = Math.max(max3, Math.max(max2, horizen));
            }
        }
        System.out.println(Math.max(max1, Math.max(max2, max3)));
        
    }

    
}
```

### **수정 및 보완 내용**

- 분석 단계에서 놓쳤던 제약 조건이나 엣지 케이스: max1를 저장할때 max1과도 비교를 한 뒤에 저장을 했어야 했는데 그걸 놓쳐서 결과가 달랐었음
- 수학적/논리적 설계에서 발생한 오류:

### **AI 최적화 코드 비교 및 진단**
	GPT피셜 최적의 코드
