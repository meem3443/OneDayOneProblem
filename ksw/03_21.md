## **📝 일일 문제 분석 및 풀이 노트**

### **[문제 이름/번호] 분석**
https://www.codetree.ai/ko/trails/complete/curated-cards/challenge-number-of-happy-sequence/description
### 행복한 수열의 개수

### **1. 문제 핵심 요약**

- [ ] 입력은 무엇인가? (자료형, 개수): 정수 두개와 정수형 이차원 배열
- [ ] 출력은 무엇인가? (포맷, 정답 조건): 행복한 수열의 수
- [ ] 문제의 목표를 한 문장으로 정리하면?: 가로 새로 줄에서 연속으로 같은 값 M개가 있는 줄의 개수

### **2. 제약 조건 (Constraints) 분석**

- 시간 제한: 3000ms__초 (약 __억 번 연산 가능)
- 메모리 제한: 176____MB
- 입력 크기(N):
    - 최소:
    - 최대:
- 값의 범위: (Integer 범위 초과 여부 확인)

### **3. 알고리즘 선택 & 복잡도 예상**

- [ ] 예상 시간 복잡도: O( n * n)
- [ ] 선택한 자료구조: 배열
- [ ] 선택한 알고리즘: (Greedy, DP, BFS/DFS, 이분탐색 등) 브루트포스
- [ ] 근거: (N의 크기를 볼 때 이 알고리즘이 적합한가?)

### **4. 수학적/논리적 증명 및 설계 (Core Logic)**

> 코드로 바로 넘어가기 전, 문제를 해결하는 핵심 로직을 수학적 공식이나 논리적 증명으로 먼저 구성합니다.

- **수식 또는 상태 전이 방정식:**
- **논리적 타당성 증명:**

### **5. 엣지 케이스 (Edge Cases) & 예외 처리**

- [ ] 오버플로우 가능성 (int vs long long/BigInt): index의 범위를 넘을 수도 있음
- [ ] M의 값이 1일 경우 따로 값을 출력해 주어야 함

### **6. 로직 설계 (Pseudo-code)**
 이차원 배열 저장
 가로줄 새로줄을 나눠서 개수를 계산
---

### **💻 코드 구현 및 피드백**

### **내 코드 (초안)**

**코드 스니펫**

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        
        int N = Integer.parseInt(st.nextToken());  
        int M = Integer.parseInt(st.nextToken());

        int[][] num = new int[N][N];

        for(int i = 0; i < N; i++){
            st = new StringTokenizer(br.readLine());
            for(int j = 0; j < N; j++){
                num[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        if(M == 1){
            System.out.println(2 * N);
            return;
        }

        int count = 0;

        for(int i = 0; i < N; i++){
            int cnt = 1;
            for(int j = 1; j < N; j++){
                if(num[i][j] == num[i][j-1]){
                    cnt++;
                } else {
                    cnt = 1;
                }

                if(cnt >= M){
                    count++;
                    break;
                }
            }
        }

        for(int j = 0; j < N; j++){
            int cnt = 1;
            for(int i = 1; i < N; i++){
                if(num[i][j] == num[i-1][j]){
                    cnt++;
                } else {
                    cnt = 1;
                }

                if(cnt >= M){
                    count++;
                    break;
                }
            }
        }

        System.out.println(count);
    }
}
```

### **수정 및 보완 내용**

- 분석 단계에서 놓쳤던 제약 조건이나 엣지 케이스:  1이 입력될때는 나중에 깨달아서 답을 맞추기 까지 오래 걸림
- 수학적/논리적 설계에서 발생한 오류: 원래 재귀함수를 만들어서 계산을 했었는데 수정을 하다 보니 코드가 너무 더러워져서 반복문만 사용해서 케이스를 구함

### **AI 최적화 코드 비교 및 진단**

- **개선된 코드의 효율성 차이:** (시간/공간 복잡도 측면에서 어떻게 개선되었는가?)
- **자가 진단:** 이 문제에서 내가 정확히 이해하지 못했던 개념이나 논리적 맹점은 무엇이었는가?