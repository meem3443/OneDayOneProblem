# [Lesson 9. 반복문 안의 if - 369 게임] 분석

## 1. 문제 핵심 요약

- [ ] 입력은 무엇인가? (자료형, 개수): 첫 번째 줄에는 N이 주어집니다.
- [ ] 출력은 무엇인가? (포맷, 정답 조건):
      1부터 N까지의 결과를 순서대로 공백을 사이에 두고 출력합니다.
      각각의 수에 대해 3의 배수이거나 수에 3, 6, 9 중 하나가 들어간다면 0을 출력하고,
      그렇지 않으면 해당 수를 그대로 출력합니다.
- [ ] 문제의 목표를 한 문장으로 정리하면?:
      1부터 N까지의 결과를 모두 출력하는데, 3, 6, 9가 들어간다면 0으로 바꾸기

## 2. 제약 조건 (Constraints) 분석

- 시간 제한: 3000ms
- 메모리 제한: 176MiB
- 입력 크기(N):
  - 1<=N<=100
- 값의 범위: Integer

## 3. 알고리즘 선택 & 복잡도 예상

- [ ] 예상 시간 복잡도: 
- [ ] 선택한 자료구조: 
- [ ] 선택한 알고리즘: 
- [ ] 근거:

## 4. 엣지 케이스 (Edge Cases) & 예외 처리

- [ ] 

## 5. 로직 설계 (Pseudo-code)

1. for i가 1부터 N보다 작거나 같을 때까지 1씩 더함
2. if i을 10으로 나머지 연산한 결과가 3, 6, 9 중 하나라면 0 출력
3. else if i을 10으로 나눈 결과를 10으로 나머지 연산한 결과가 3, 6, 9 중 하나라면 0 출력
4. else if i을 3으로 나머지 연산한 결과가 0이라면 0 출력
5. else i 그대로 출력
