# *N번 더하기*
# **Trail.0 Chapter4. Lesson.1**

# *링크: https://www.codetree.ai/ko/trails/complete/curated-cards/nl-pre-loop-basics-3/description*

# ------------------------------------------------------------------------------------------------------

# 문제 출력하기

# 1) 정수 A 와 N이 첫 번째 줄에 공백을 두고 입력한다.
# 2) 이때, A에 N을 더하는 과정을 N번 반복한다. (처음 input()을 통해 작성한 N을 반복문에 집어 넣기 때문)
# 3) 그렇게 나온 연산 결과 값을 순서대로 출력한다.

# 그렇다면 결과적으로는 A라는 변수에 N을 더해서 나온 값은 결국 A라는 변수이기 때문에
# 이 과정을 N번 반복하는 식이 필요하다.
# 그렇기에
# A = A + N 이라는 식을 사용할 수 있다. 

# 또한, 반복문을 사용하기 전에는 먼저 입력 코드를 작성해야 한다. 그렇기 위해서는 지금까지 사용하며 배웠던 'map' 과 'split()' 코드를 사용해야 한다.

# 다신 한 번더 기억을 상기 시킨다면, 'map' 코드는 (함수, 리스트)를 통해 리스트에 있는 모든 요소를 함수로 저장할 수 있다. 그리고 'split()'를 통해서 input()에 넣은 입력 값을 공백이 있어도 인식 할 수 있다.

# A, N = map(int, input().split())

# 위 식을 작성하고, 그 다음에 변수 A에 대한 식을 작성한다. 
# A = A + N
# 그 다음 반복문을 작성해야 할까?

# 실험한다.

# ....

# 실험 결과 실패했다.

# 그렇게 뭐가 문제일까 생각해보니 N의 반복을 위해서는 반복문 내에 집어 넣어야 한다.
# 그래야 그 식 그대로 반복문이 실행된다.
# for i in range(N):
#       A = A + N
#       print(A)
# 이 상태로 A를 출력하면 내가 원하는 공식의 
# 답이 출력된다.
