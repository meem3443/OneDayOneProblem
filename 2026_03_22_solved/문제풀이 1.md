# *일의 자리 배열*
# **Trail. 0 Chapter 6. Lesson 2**

# *링크: https://www.codetree.ai/ko/trails/complete/curated-cards/nl-pre-1d-array-iteration-3/description*

# 문제 풀이

# 1) 첫 번째 항과 두 번째 항이 공백을 두고 차례로 입력된다.
# 2) 세 번째 항부터 "전전항 과 전항의 합"을 구한다.
# 3) 그리고 그렇게 합한 항들의 1번째 자리를 출력한다.
# 4) 이를 10번 반복한다.

# 먼저 2)의 내용을 수학적 식으로 작성해 보다 더 이해하려고 한다.
# 그래야 내가 생각하지 못한 무언가가 떠오를 듯 싶었다.

# [ n, n2]
# -> n+n2 = n3
# -> n2 + n3 = n4
# -> n3 + n4 = n5
# ...

# 이렇게 쭉 해서 숫자만 더하고 이를 그대로 출력한다면 문제가 되지 않는다고 생각한다. 하지만 어떻게 일의 자리만 출력해야 하는가 이다.

# 기존에 사용했던 f-strings는 소수점에만 해당하는 것이기 때문에 사용한다고 한들 내가 바라는 것을 출력할 수 없다.

# 그렇기에 수학적 계산식을 이용하는 것이다. 어떤 수를 10으로 나누었을 때, 그 수의 나머지는 반드시 기존 숫자의 1의 자리가 나온다. 그렇다면, 이를 이용해서 1의 자리만 출력할 수 있도록 만들 수 있다.

# 위에 작성한 n의 식을 성립시키기 위해서는 어떻게 코드를 작성해야 할까.

# 맨 처음 작성한
# n1, n2 = map(int, input().split())
# 이다. 

# 이 코드는 아직 리스트로 정리되어 있는게 아니다.
# 물론, 이 숫자를 더한 값을 나열해 10번을 할 수 있지만, 그렇게 되면
# 코드가 길어지고 복잡해지기 때문에 별로 좋지 못하다.

# 그렇기에 조금 더 간단하게 하기 위해서 반복문을 사용해야 하며, 
# 리스트에 있는 각 위치에 따라 값을 더할 예정이다.
# 위의 가정은 잘못 됐다. 다시 작성한다.

# 우선 리스트의 특성을 이용해서 코드를 풀어나갈 예정이다.

# 그렇기에
# n1, n2 = list((map(int, input().split()))
# 로 작성한다.

# 그러나 이는 리스트의 열에 들어갈 수이다.
# 리스트 형태의 변수를 하나 더 지정해서 작성한다.
# arr = [ ]
# 그리고 그 형태 박스 안에 n1과 n2 를 넣는다.
# arr.append(n1)
# arr.sppend(n2)

# 이렇게 된다면 박스의 0번과 1번 칸에는 n1 과 n2 가 들어가 있는다.

# 그리고 문제를 확인하면 총 10개의 칸에 각 일의 자릿수가 들어가고
# 동시에 2번 칸 부터 채워야 한다. 0번과 1번 칸에는 이미 수가
# 들어가 있어서 제외한다. 그렇기에 범위를 (2, 10)으로 지정한다.

# 그리고 for 반복문을 활용해 범위까지 정했다면, 다시금
# arr.append()
# 를 이용해서 나머지 2번부터 10번 칸의 수를 채워야 한다.
# i라는 시작에서 전전항(-2)과 전항(-1)을 한 두 칸을 더하고 이를 
# 10으로 나눠 나머지를 구한다.

# 그렇게 하면 첫 번째 반복문이 완성된다.
# 그 다음 새로운 반복문을 사용하는 이유는 arr라는 리스트를 그대로 출력하면 리스트 형태로 입력값들이 출력하기 때문이다.
# 그래서 새롭게 사용한 반복문에서의 범위는 리스트인 arr를 넣고, 
# 이를 공백을 사이에 두고 출력한다. 
# 그렇게 된다면 문제에 대한 해결 코드가 완성된다.
