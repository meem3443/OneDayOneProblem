# *제곱하여 출력하기*
# **Trail.0 Chapter 6. Lesson 2**

# *링크: https://www.codetree.ai/ko/trails/complete/curated-cards/nl-pre-1d-array-iteration-2/description*

# 문제 풀이

# #1) 원소의 개수가 N개로 지정해야 한다. 그리고 두 번째 줄에는
# #지정해둔 N개만큼 원소가 공백을 두고 주어진다.
# 2) 그 입력한 원소를 각각 제곱한다.
# 3) 제곱한 원소를 공백을 두고 출력한다.(리스트 형태 X)

# 먼저 원소의 개수인 N을 입력할 코드를 작성한다. 그 다음에
# N개 만큼의 원소가 공백을 두고 줘야 한다.

# 그렇다면 우선 저번에 사용한 list를 사용해야 한다고 생각한다.
# list 코드를 사용해서 for 반복문을 사용하면 list에 있는 원소를 
# 하나씩 꺼내서 저장하고 이를 출력하는 것을 반복한다.

# 이때 만약에 제곱을 넣지 않는다면, 입력한 수 만큼 그대로 출력이 된다.

# #원소의 개수
# N = int(input())

# #N개의 원소
# num = list(input().split())

# for i in range(N):
#     print(num[i], end= " ")

# 하지만 여기서 제곱수를 그냥 넣게 된다면 코드는 오류가 난다.

# 그렇기에 이 제곱수를 어떻게 넣냐 num를 정수라고 입력해야한다. 그러기 위해서는
# print(int(num[1])***2)
# 이렇게 작성해야 num에 정수를 입력할 수 있다.

# 이렇게 작성한 다음 end를 이용하면 공백을 넣을 수 있다.
